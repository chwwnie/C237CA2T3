<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;600;700;800&display=swap" rel="stylesheet">
  <link href="/css/style.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>My Applications - Pet Shelter Portal</title>
</head>
<body>
  <%- include('header') %>

  <div class="container mt-4">
    <% if (typeof errors !== 'undefined' && errors && errors.length > 0) { %>
      <div class="alert alert-danger">
        <% errors.forEach(function(e) { %><p class="mb-0"><%= e %></p><% }); %>
      </div>
    <% } %>
    <% if (typeof messages !== 'undefined' && messages && messages.length > 0) { %>
      <div class="alert alert-success">
        <% messages.forEach(function(m) { %><p class="mb-0"><%= m %></p><% }); %>
      </div>
    <% } %>
    <div class="page-heading">
      <div class="icon-badge">📋</div>
      <div>
        <h2>My Applications</h2>
        <p>Track the status of your adoption requests.</p>
      </div>
    </div>
    <% if (applications.length === 0) { %>
      <div class="empty-state">
        <div class="icon">📋</div>
        <p>No applications yet</p>
        <p class="small">Apply to adopt a pet and it'll show up here.</p>
        <a href="/pets" class="btn btn-primary mt-2">Browse Pets</a>
      </div>
    <% } %>
    <% if (applications.length > 0) { %>
    <table class="table table-hover text-center align-middle">
      <thead>
        <tr>
          <th>Pet</th>
          <th>Species</th>
          <th>Stage</th>
          <th>Submitted On</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        <% applications.forEach(a => { %>
          <tr>
            <td><a href="/pets/<%= a.petId %>"><%= a.petName %></a></td>
            <td><%= a.petSpecies %></td>
            <td>
              <span class="badge <%=
                  a.stage === 'Approved' ? 'bg-success' :
                  a.stage === 'Rejected' ? 'bg-danger' :
                  'bg-secondary'
                %>"><%= a.stage %></span>
            </td>
            <td><%= a.createdAt.toDateString() %></td>
            <td>
              <% if (a.stage === 'Submitted') { %>
                <form action="/my-applications/<%= a.id %>/withdraw" method="POST"
                      onsubmit="return confirm('Withdraw this application?')">
                  <button type="submit" class="btn btn-sm btn-outline-secondary">Withdraw</button>
                </form>
              <% } %>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>
    <% } %>
  </div>
</body>
</html>
